# ─────────────────────────────────────────────────────────────
# .gitignore ของโครงงาน LeaveEasy
#
# ไฟล์นี้คือ "รายชื่อสิ่งที่ห้ามส่งขึ้น GitHub"
#
# ⚠️ ต้องสร้างไฟล์นี้ **ก่อน** push ขึ้น GitHub ครั้งแรกเสมอ
#    ถ้า push ไปแล้วค่อยมาสร้าง ของที่หลุดไปแล้วจะยังอยู่ในประวัติ
# ─────────────────────────────────────────────────────────────


# ── ชิ้นส่วนที่ npm ติดตั้งให้ (ใหญ่มาก และสร้างใหม่ได้เสมอ) ──
node_modules/


# ── 🔑 ของลับ ห้ามหลุดเด็ดขาด ──
# API key ของ OpenRouter (สัปดาห์ที่ 8) และค่าตั้งค่าที่เป็นความลับ
config.js
js/config.js
secrets.js
.env
.env.local
.env.*

# ค่าตั้งค่า Firebase ของแต่ละคน (ในโครงงานนี้เก็บไว้ในเครื่องตัวเอง)
js/firebase-config.js
.firebaserc

# ไฟล์คีย์ระดับผู้ดูแลของ Firebase
*serviceAccount*.json
*firebase-adminsdk*.json
.firebaserc.local


# ── ไฟล์ที่ Firebase สร้างเอง ──
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log


# ── ไฟล์ที่เครื่องเราสร้างเอง ──
.DS_Store
Thumbs.db
desktop.ini
*.log
.vscode/
.idea/


# ── ผลการทดสอบ (สัปดาห์ที่ 9) ──
test-results/
playwright-report/
blob-report/


# ─────────────────────────────────────────────────────────────
# ✅ วิธีตรวจว่าใช้ได้จริง — พิมพ์คำสั่งนี้ก่อน push ทุกครั้ง
#
#     git status
#
# แล้วอ่านรายชื่อไฟล์ที่จะถูกส่งขึ้นไป
# ถ้าเห็น node_modules หรือไฟล์ที่มีคำว่า key / secret / config
# แปลว่ายังกันไม่สำเร็จ — อย่าเพิ่ง push ให้เรียกผู้ช่วยก่อน
#
# 🆘 ถ้าเผลอ push ของลับขึ้นไปแล้ว
#    1. เพิกถอน (revoke) คีย์ใบนั้นทันที — ทำข้อนี้ก่อนเสมอ
#    2. ค่อยไปลบไฟล์ออกจาก repo
#    การลบ commit อย่างเดียวไม่พอ เพราะคีย์ถูกคัดลอกไปแล้ว
# ─────────────────────────────────────────────────────────────
