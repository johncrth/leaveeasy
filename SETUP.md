# 🛠️ SETUP — เตรียมเครื่องให้ LeaveEasy ทำงานได้

⏱ ประมาณ 10 นาที · ทำครั้งเดียวจบ

> 📌 **ทำไมต้องทำขั้นตอนนี้**
> ไฟล์ค่าตั้งค่าของ Firebase และคีย์ AI **ไม่ได้อยู่ใน repo** เพราะเป็นของลับที่ห้ามส่งขึ้น GitHub
> ใน repo มีแต่ไฟล์ตัวอย่างลงท้ายด้วย `.example.js` — คุณต้องคัดลอกแล้วเติมค่าของตัวเอง
>
> ⛳ ถ้าคุณเปิดดูแท็ก `week6-start` เฉย ๆ **ข้ามไฟล์นี้ไปได้เลย** เพราะแท็กนั้นยังไม่ต่อฐานข้อมูล

---

## ขั้นที่ 0 — สิ่งที่ต้องมีในเครื่อง ⏱ ~1 นาที

- **Node.js** (ติดตั้งแล้วตั้งแต่ Module 1) — ตรวจด้วยการพิมพ์ `node -v` ใน terminal
  **จะเห็น:** ตัวเลขเวอร์ชัน เช่น `v20.11.0`
- **บัญชี Google** สำหรับเข้า Firebase

จากนั้นเปิด terminal ที่โฟลเดอร์โครงงาน แล้วพิมพ์

```bash
npm install
```

**จะเห็น:** ข้อความ `added ... packages` ใช้เวลา 1–2 นาที · ปล่อยให้ทำจนหยุด

---

## ขั้นที่ 1 — สร้าง Firebase project ⏱ ~3 นาที

1. เปิด <https://console.firebase.google.com> แล้วเข้าสู่ระบบด้วยบัญชี Google
2. กดปุ่ม **Add project** (เพิ่มโปรเจกต์) ที่กลางหน้าจอ
3. ตั้งชื่อโปรเจกต์ เช่น `leaveeasy-ชื่อกลุ่มคุณ` แล้วกด **Continue**
4. หน้า Google Analytics ให้ **ปิดสวิตช์** แล้วกด **Create project**

   **จะเห็น:** วงกลมหมุนประมาณ 30 วินาที แล้วขึ้นว่า *Your new project is ready*

---

## ขั้นที่ 2 — เปิดใช้งาน Firestore (ฐานข้อมูล) ⏱ ~2 นาที

1. เมนูซ้ายมือ กด **Build** → **Firestore Database**
2. กดปุ่ม **Create database**
3. เลือกที่ตั้งเซิร์ฟเวอร์ (location) เป็น `asia-southeast1` แล้วกด **Next**
4. เลือก **Start in test mode** (โหมดทดสอบ) แล้วกด **Create**

   **จะเห็น:** ตารางว่าง ๆ พร้อมข้อความ *Start collection*

> ⚠️ **โหมดทดสอบเปิดให้ทุกคนอ่านเขียนได้ และหมดอายุใน 30 วัน** — จดวันหมดอายุไว้
> ในแท็ก `week7-end` เป็นต้นไปมีไฟล์ `firestore.rules` ที่ใส่กฎจริงแทนแล้ว (ดูขั้นที่ 6)

---

## ขั้นที่ 3 — เปิดใช้งาน Authentication (ล็อกอิน) ⏱ ~1 นาที

> จำเป็นตั้งแต่แท็ก `week7-end` เป็นต้นไป

1. เมนูซ้ายมือ กด **Build** → **Authentication** → ปุ่ม **Get started**
2. ในรายการวิธีล็อกอิน เลือก **Email/Password**
3. เปิดสวิตช์ **Enable** อันบนสุด แล้วกด **Save**

   **จะเห็น:** แถว *Email/Password* ขึ้นสถานะว่า **Enabled**

---

## ขั้นที่ 4 — คัดลอกค่าตั้งค่ามาใส่ในโครงงาน ⏱ ~2 นาที

1. กดรูป **⚙️ เฟือง** ข้างคำว่า *Project Overview* มุมซ้ายบน → **Project settings**
2. เลื่อนลงล่างสุดถึงหัวข้อ **Your apps** → กดไอคอน **`</>`** (Web)
3. ตั้งชื่อเล่นแอปว่า `leaveeasy-web` → กด **Register app** (ยังไม่ต้องติ๊ก Hosting)
4. หน้าจอจะขึ้นโค้ดชุดหนึ่งที่มีคำว่า `const firebaseConfig = {` — **คัดลอกเฉพาะค่าในวงเล็บปีกกา**

จากนั้นในโฟลเดอร์โครงงาน

```bash
cp js/firebase-config.example.js js/firebase-config.js
```

> 💡 บน Windows ถ้าคำสั่ง `cp` ใช้ไม่ได้ ให้คัดลอกไฟล์ด้วยมือใน File Explorer แล้วเปลี่ยนชื่อ

เปิดไฟล์ `js/firebase-config.js` แล้ว **แทนค่าทุกช่องด้วยค่าที่คัดลอกมา**

**จะเห็น:** ไฟล์หน้าตาแบบนี้ (ค่าเป็นของโปรเจกต์คุณเอง)

```js
export const firebaseConfig = {
  apiKey: "...",
  authDomain: "leaveeasy-xxxx.firebaseapp.com",
  projectId: "leaveeasy-xxxx",
  storageBucket: "leaveeasy-xxxx.appspot.com",
  messagingSenderId: "...",
  appId: "..."
};
```

> 🔑 **ไฟล์ `js/firebase-config.js` ถูก `.gitignore` กันไว้แล้ว ห้าม commit**
> ตรวจด้วยการพิมพ์ `git status` — **ต้องไม่เห็นชื่อไฟล์นี้ในรายการ**

---

## ขั้นที่ 5 — เปิดเว็บและใส่ข้อมูลตัวอย่าง ⏱ ~1 นาที

```bash
npm run dev
```

**จะเห็น:** terminal บอกที่อยู่ เช่น `http://localhost:3000` — เปิดที่อยู่นั้นในเบราว์เซอร์

1. เข้าหน้า `http://localhost:3000/seed.html`
2. กดปุ่ม **ใส่ข้อมูลตัวอย่างลง Firestore**

   **จะเห็น:** รายการบรรทัดวิ่งขึ้นมา และบรรทัดสุดท้ายบอกจำนวนไฟล์ที่ใส่สำเร็จ

3. กลับไปที่ **Firebase Console → Firestore Database**

   **จะเห็น:** โฟลเดอร์ `users` `leaveTypes` `leaveRequests` โผล่ขึ้นมาพร้อมข้อมูล

> 🆘 **ถ้าขึ้น permission denied** — แปลว่ากฎยังไม่เปิดให้เขียน
> ถ้าอยู่ในแท็ก `week7-end` ขึ้นไป ต้อง **สมัครสมาชิกและล็อกอินก่อน** แล้วค่อยกดปุ่ม seed
> (แท็ก `week8-end` ขึ้นไป ต้องเปิดสิทธิ์ `manager` ให้ตัวเองก่อน ดูขั้นที่ 7)

---

## ขั้นที่ 6 — นำขึ้นออนไลน์ด้วย Firebase Hosting ⏱ ~3 นาที

> จำเป็นตั้งแต่แท็ก `week7-end` เป็นต้นไป · ข้ามได้ถ้าอยากทดสอบในเครื่องอย่างเดียว

```bash
npm install -g firebase-tools
firebase login
cp .firebaserc.example .firebaserc
```

เปิดไฟล์ `.firebaserc` แล้วแทน `ใส่-project-id-ของคุณ` ด้วย **Project ID** ที่เห็นในหน้า Project settings

จากนั้น

```bash
firebase deploy
```

**จะเห็น:** ข้อความวิ่งขึ้นมาประมาณ 10 บรรทัด และบรรทัดสุดท้ายเขียนว่า
`Deploy complete!` พร้อมที่อยู่ **Hosting URL** — นั่นคือ URL ที่ส่งให้คนอื่นเปิดได้

> 📌 คำสั่ง `firebase deploy` ส่งขึ้นไปทั้ง **หน้าเว็บ** และ **กฎเฝ้าข้อมูล** (`firestore.rules`)
> แก้กฎในเครื่องอย่างเดียวไม่พอ — ต้อง deploy ทุกครั้ง

---

## ขั้นที่ 7 — ตั้งบทบาทให้บัญชีตัวเอง ⏱ ~1 นาที

> จำเป็นตั้งแต่แท็ก `week8-end` เป็นต้นไป เพราะกฎรายห้องแยกสิทธิ์ตามช่อง `role`

บัญชีที่สมัครใหม่ทุกบัญชีได้ `role` เป็น `employee` เสมอ
ถ้าอยากทดลองเป็น **ผู้อนุมัติ** ให้เปลี่ยนด้วยมือ

1. เปิด **Firebase Console → Firestore Database → โฟลเดอร์ `users`**
2. กดเข้าไฟล์ที่ชื่อตรงกับ uid ของคุณ
3. แก้ช่อง `role` จาก `employee` เป็น `manager` แล้วกด **Update**
4. รีเฟรชหน้าเว็บ

   **จะเห็น:** เห็นใบลาของทุกคน และปุ่มอนุมัติ / ไม่อนุมัติกดได้

---

## ขั้นที่ 8 — คีย์ OpenRouter สำหรับปุ่มผู้ช่วย AI ⏱ ~1 นาที

> จำเป็นตั้งแต่แท็ก `week8-end` เป็นต้นไป · ไม่ใส่คีย์ ระบบยังใช้งานได้ตามปกติ แต่ปุ่ม AI จะขึ้นข้อความว่ายังไม่ได้ตั้งค่า

```bash
cp js/config.example.js js/config.js
```

เปิด `js/config.js` แล้วใส่คีย์ที่ **ผู้สอนแจกให้กลุ่มของคุณ**

> 🔑 **ไฟล์ `js/config.js` ถูก `.gitignore` กันไว้แล้ว ห้าม commit เด็ดขาด**
> ตรวจด้วย `git status` — ถ้าเห็นชื่อไฟล์นี้ **หยุดทันที อย่า commit** แล้วเรียกผู้ช่วย
>
> ⚠️ **คีย์ที่อยู่ในหน้าเว็บ คนอื่นเปิดดูได้** — ยอมรับได้เฉพาะในคาบเรียนเพราะผู้สอนตั้งวงเงินไว้
> **ไม่ใช่วิธีที่ถูกต้องสำหรับระบบจริง**

---

## 🆘 ติดตรงนี้ให้ทำอย่างไร

| อาการ | ทำอย่างไร |
|---|---|
| หน้าเว็บขึ้นแถบเหลือง "ยังไม่ได้ตั้งค่า Firebase" | ยังไม่ได้สร้างไฟล์ `js/firebase-config.js` — กลับไปทำขั้นที่ 4 |
| `permission denied` ตอนอ่านหรือเขียนข้อมูล | 3 สาเหตุเรียงจากบ่อยที่สุด: **ยังไม่ได้ล็อกอิน** · **กฎยังไม่ได้ deploy** (`firebase deploy`) · โหมดทดสอบหมดอายุแล้ว |
| เปิด `index.html` ตรง ๆ แล้วหน้าว่างเปล่า | ตั้งแต่ `week6-end` ต้องเปิดผ่าน `npm run dev` — เปิดไฟล์ตรง ๆ เบราว์เซอร์จะบล็อกโมดูล |
| `npm run dev` ขึ้นว่า port ชน | พิมพ์ `npx serve -l 5500 .` แล้วเปิด `http://localhost:5500` แทน |
| ข้อมูลอยู่ใน Console แต่ไม่ขึ้นบนหน้าจอ | ตรวจการสะกดชื่อโฟลเดอร์ — Firestore แยกตัวพิมพ์เล็ก-ใหญ่ `leaveRequests` กับ `leaverequests` คือคนละโฟลเดอร์ |
| ล็อกอินแล้วแต่ไม่เห็นใบลาของคนอื่น | ถูกต้องแล้วสำหรับ `week8-end` — ต้องเปลี่ยน `role` เป็น `manager` ตามขั้นที่ 7 |
| ปุ่ม AI ขึ้นว่าเรียกไม่สำเร็จตลอด | ตรวจ 3 อย่างตามลำดับ: คีย์ในไฟล์ `js/config.js` ถูกใบไหม · ชื่อโมเดลสะกดถูกไหม · วงเงินของคีย์หมดหรือยัง (ถามผู้สอน) |
